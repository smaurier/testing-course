// invitation.contract.ts — PAGE BLANCHE. Le contrat consumer-driven : CE QUE LE FRONT EXIGE
// de la réponse `GET /api/invitations/:id`, et rien de plus. C'est le front (consumer) qui
// définit ce fichier — le provider s'y conforme, pas l'inverse.
// Exports attendus : InvitationDTO, validateInvitationContract.

export {};
