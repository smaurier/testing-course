// invitation.ts — PAGE BLANCHE. Construis ce module en TDD strict (RED → GREEN → REFACTOR),
// un cycle à la fois. Ne triche pas en écrivant l'implémentation avant le test qui l'exige.
// L'oracle (test/) est le juge final, mais il ne remplace pas tes propres cycles : tiens un
// journal dans CYCLES.md (voir README § Étapes) — le correcteur le lit.
//
// Exports attendus : MemberStatus, Member, InvitationRefus, InvitationResult, COOLDOWN_JOURS,
// evaluerInvitation.

export {};
